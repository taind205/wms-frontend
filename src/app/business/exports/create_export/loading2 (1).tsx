'use client' 
import LoadingSkeleton1 from "@/app/components/loading_UI";
import { Skeleton } from "@mui/material";

export default function Loading() {
  // You can add any UI inside Loading, including a Skeleton.
  return <LoadingSkeleton1/>
}